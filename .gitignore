/target
**/*.rs.bk
examples/**/.progress
examples/**/AGG_RESUME.md
.agg/
# internal scratch / working notes, never public — all live under internal/
internal/

# debugging/test scratch that `git add -A` has swept in twice already
*.out
*.raw
*.bak
*.orig
typescript

# Python bytecode cache — web_e2e.py generates scripts/__pycache__/ on every e2e run
__pycache__/
*.pyc
