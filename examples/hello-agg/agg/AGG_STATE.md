<!-- AGG_STATE.md — the standing instructions fed to EVERY fresh worker session (and the forward
     state the agent maintains). You start it; the agent updates it as it works. -->

# Goal
Fix `add.py` so that running `python3 add.py` prints exactly: 2

# This session
1. Run `python3 add.py` and see what it prints.
2. Edit `add.py` so it prints 2.
3. You are autonomous — do the work, commit, and exit.
