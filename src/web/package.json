{
  "name": "agg-web",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone web interface for AgenticGoGo — monitor and steer a running agg loop. Talks to `agg serve` over HTTP.",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^3.3.1",
    "@sveltejs/adapter-node": "^5.2.9",
    "@sveltejs/kit": "^2.8.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.1.0",
    "vite": "^5.4.0"
  }
}
